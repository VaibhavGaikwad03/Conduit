<?xml version="1.0" encoding="utf-8"?>
<device-admin xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-policies>
        <!-- Allows DevicePolicyManager.lockNow() so the PC can lock the phone. -->
        <force-lock />
    </uses-policies>
</device-admin>
